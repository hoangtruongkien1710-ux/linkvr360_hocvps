services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_DB: tour360
      POSTGRES_USER: tour360
      POSTGRES_PASSWORD: tour360123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
